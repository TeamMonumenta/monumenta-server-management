# Minimal config for a throwaway server that exits during plugin onLoad, before any world loads.
# Copied into the run directory by run_java_test.py.
online-mode=false
max-players=0
level-type=minecraft\:flat
spawn-protection=0
white-list=false
